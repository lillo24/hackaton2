# Lillo Map

Status: `DRAFT` because this workspace now contains a runnable frontend MVP, but it still has no automated tests and is intended for fast iteration.

Files and folders
- `README.md` - folder map for the `lillo/` workspace.
- `history-implementations/` - prompt and implementation-history material used as the source brief for the app work.
- `app-skeleton/` - self-contained Vite React implementation of the agriculture alert MVP and the adapted phone frame.

Why these live together
- `lillo/` owns one contributor's implementation track.
- The history folder captures the prompts; the app folder contains the actual code produced from those prompts.
