You have access to:
1. the current repo for the new Challenge 1 application
2. ui_repo_config.txt
3. an old repo called "Venice Care Prototype"

Task:
Extract and adapt the visual "Phone Frame" from the old repo into the new application, while keeping the implementation aligned with ui_repo_config.txt.

Important:
Do NOT migrate the old project wholesale.
Do NOT port the old pages, old content, old navigation, or unrelated components unless strictly needed.
Treat the old repo only as a visual/component donor.

Main goal:
Bring the cool phone frame visual shell into the new agriculture alert MVP so the app feels more polished and demo-ready.

What to inspect in the old repo:
- src/components/PhoneFrame.jsx
- src/styles/PhoneFrame.css

You may inspect other files only if needed to understand sizing or layout behavior, but avoid copying old app logic.

What to build in the new app:
Create a reusable PhoneFrame component for the new project that:
- preserves the strong visual feeling of the old frame
- works with the new app’s style direction from ui_repo_config.txt
- feels modern, premium, and animation-friendly
- can wrap arbitrary page content
- is easy to reuse on multiple pages if needed

Implementation rules:
- keep the new app aligned with the repo config
- prefer the current new project stack and conventions
- adapt the old JSX/CSS approach if needed, do not blindly preserve it
- keep the component modular and easy to style
- use mock content only
- prioritize visual result over architectural complexity

Design adaptation requirements:
- keep the overall phone silhouette / bezel effect
- improve it where useful so it feels cleaner and more premium
- make sure it fits the agriculture-monitoring aesthetic of the new project
- avoid making it feel like a leftover from the healthcare prototype
- support nice shadows, gradients, and polished spacing
- if useful, add subtle animation/micro-interaction, but keep it tasteful

Layout requirement:
The phone frame should become the main visual container for the app preview.
The new agriculture app content should render inside the phone frame.
Outside-the-phone controls from the old repo should NOT be migrated unless they clearly help the new MVP.

Responsiveness:
- keep the phone frame visually strong on desktop
- avoid breaking smaller screens
- it is acceptable for the phone frame to scale down responsively
- do not hard-lock the whole app into a bad desktop experience

Scope boundaries:
Migrate/adapt:
- PhoneFrame component
- any minimal supporting styles/helpers needed

Do not migrate:
- old page copy/content
- old healthcare imagery
- old carousel unless explicitly reused for the farm-type selector
- old navigation/sidebar logic
- old business logic

Nice optional reuse:
If the old repo has motion or presentation ideas that help the new “Farm Type selection” screen, you may reuse the interaction pattern, but only if it fits the new product and does not slow implementation.

Expected output:
1. Add the new reusable PhoneFrame component
2. Integrate it into the new app shell or main preview area
3. Make sure the current/new pages render correctly inside it
4. Keep the style coherent with ui_repo_config.txt
5. Briefly summarize:
   - what was reused from the old repo
   - what was rewritten
   - what was intentionally not migrated

Decision rule:
When in doubt, prefer:
- adapting the visual idea
over
- copying the old implementation literally

Success criteria:
- the new app keeps the hackathon config direction
- the phone frame looks cool and intentional
- the app feels like a polished agriculture alert prototype
- the migrated piece is clean enough to extend later