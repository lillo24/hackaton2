# History Implementations Map

Status: `FROZEN` because these files are prompt/history inputs rather than active runtime code.

Files
- `README.md` - folder map for the history inputs.
- `01_app_skeleton.md` - app-shell brief describing the base routes, reusable layout, and mock-data MVP scope.
- `prompt_extract_phone_frame.txt` - follow-up brief that asks for the Venice Care phone frame to be adapted into the new agriculture UI.

Why these live together
- This folder records the implementation prompts in the order they were used.
- The runtime implementation is intentionally kept outside this folder in `../app-skeleton/`.
